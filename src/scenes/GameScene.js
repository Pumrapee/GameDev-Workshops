import Phaser from "phaser";

class GameScene extends Phaser.Scene {
    constructor(test) {
        super({
            key: "GameScene",
        });
    }

    preload() {
        // code here
    }

    create() {
        // code here
    }

    update(delta, time) {
        // code here
    }
}
export default GameScene;

